id: synthesis_slides
tier: 'synthesize'
name: Synthesis — Slides / Talk Deck (AI-authored Touying)
version: '5.0.0'
schema_version: '3.0'
scope_tags:
  domain: [any]
  audience: [auditor]
  workflow_shape: [any]
last_reviewed: '2026-06-16'
see_also:
- synthesis/deliverable_design        # the design skill: slide-layout + palette selection
- synthesis/defense_prep              # a deck for a defense needs the Q&A prep
- synthesis/synthesis_paper           # the paper the talk compresses
description: |
  Guide the AI to author `synthesis/slides.typ` directly (Touying
  template) for a talk — lab meeting, conference, defense, invited
  seminar, or job talk. Research-OS does not generate slide decks —
  the AI does. tool_typst_compile renders synthesis/slides.pdf; for
  HTML decks, author synthesis/slides.html directly.

  **What a talk is.** A sequential, paced presentation. The deck
  alone is not the talk — the SPEAKER carries the story, the slides
  support it. Speaker notes are therefore part of the deliverable,
  not a polish step.

  **Distinct from poster + dashboard.** A poster is a single static
  panel; a dashboard is interactive HTML; a deck is sequential. These
  shape the protocol's design constraints.

trigger: |
  Researcher asks: "make me slides", "build the talk", "lab meeting
  deck", "conference slides", "defense talk", "deck for tomorrow",
  "I'm presenting to <audience>".

prerequisites:
- At least one workspace/<step>/conclusions.md has substantive findings, OR synthesis/paper.typ already exists.
- "Researcher's `research_goal.output_types` (inputs/researcher_config.yaml) includes `slides`. If it doesn't, tool_synthesis_scaffold returns status=ask — confirm with the researcher before scaffolding. Empty output_types is treated as 'open'."

design_principles: |
  - One claim per slide. The title bar makes the claim, not labels
    the slide. "Random forests recover the effect" beats "Random
    forest results."
  - Readable at the back of the room. Type and axis labels are sized
    for the worst seat at the venue's projection size, not for a laptop
    preview — a figure whose grid labels are legible on screen but
    vanish on a projector belongs in the paper, not the talk. Make a
    TALK VERSION of dense figures when needed.
  - Speaker notes are mandatory. Every slide gets >=1 sentence (what
    to say, what to point at, what to skip if time is short).
  - Backup slides anticipate the questions you know are coming. A
    deck without a backup section is half-finished.
  - The arc fits the duration. 10-minute lightning != 45-minute talk
    in slide count, content density, OR depth of Q&A prep.
  - No filesystem paths visible. Audiences should never see
    `workspace/<step>/outputs/...`.

audience_guidance: |
  Slide count follows from the duration and the one-idea-per-slide
  pacing, not from a fixed number — budget roughly a minute or two per
  content slide and cut to fit. What changes per audience is the ARC
  and what earns a slide:
  - lab_meeting — informal; skip background; backup slides for the
    questions the lab will actually ask.
  - conference_lightning — ruthless one-idea-per-slide; headline
    finding on the title slide; almost no background.
  - conference_talk — include prior-work framing + a sensitivity /
    limitations slide.
  - defense — one chapter ~ one mini-arc; the backup deck is roughly
    half the work.
  - invited_seminar — story-first arc across multiple studies, not a
    paper-by-paper recap.
  - teaching — learning objectives up front; a worked problem in the
    middle; a common-mistakes slide earns its place here.

  TWO PATHS — the same findings make two genuinely different talks, and
  Research-OS gives you the STRUCTURE for each (not the design, not the
  final wording — you author + design + present those):
  - TECHNICAL (peers / a methods or dev review) — lead with the design
    and the methods, why the result is correct, validity + limitations +
    reproducibility. Depth over reach. Scaffold with
    tool_synthesis_scaffold(kind='slides', audience='technical') →
    synthesis/presentation_technical.md.
  - PUBLIC (general / cross-disciplinary / lay) — lead with the hook and
    the big theme, what it connects to, the one thing to remember.
    Meaning over method; minimal jargon. Scaffold with
    tool_synthesis_scaffold(kind='slides', audience='public') →
    synthesis/presentation_public.md.
  When a researcher needs both a peer talk and a public talk, scaffold
  BOTH structures and tailor each to its audience — don't reuse one deck
  for both. Each outline records what every section is FOR; you fill it
  for THIS project + THIS audience.

quality_standards: |
  - slide_count — enough to carry the arc at one idea per slide for the
    chosen duration; a two-slide "deck" is a stub, not a talk.
  - speaker_notes — every slide carries >=1 sentence of notes
  - citation_cap — a deck is a talk, not a bibliography; cite only the
    handful of works the audience needs named aloud.
  - no_path_leaks — zero `workspace/<step>/...` in slide-visible text
  - figure_readability — every figure passes the back-of-room test at
    the venue's projection size (legible type, no laptop-only labels)

steps:
- id: pick_audience
  name: Pick audience + duration
  description: |
    Ask the researcher in one line; default to lab_meeting / 30 min
    if not specified.

- id: inspect
  name: Inspect what's ready
  description: |
    tool_synthesis_scaffold (operation=plan) returns per-step conclusions + focal
    figures the deck can draw on.

- id: scaffold
  name: Scaffold slides.typ (in synthesis/scratch — never a numbered step)
  description: |
    A slide deck is a SYNTHESIS deliverable, not an analysis step — do
    NOT create a numbered workspace step for it. Iterate in the synthesis
    draft area, then land the finished deck in its proper home:

      tool_synthesis_scaffold(kind='slides', scratch=true)
        → synthesis/scratch/slides.typ  (gitignored draft; iterate here)

    Build + refine here until it's done. For a MEETING deck (lab meeting,
    committee, defense practice), scaffold straight into that meeting's
    folder so the deck + its figures travel together:

      tool_synthesis_scaffold(kind='slides', meeting='<YYYY-MM-DD>')
        → synthesis/meetings/<date>/slides.typ

    For the project's single canonical deck, omit both → the flat
    synthesis/slides.typ. The skeleton is a <=60-line Touying deck with
    section headers and `// AI: ...` markers. Refuses to overwrite an
    existing file. Move a finished scratch draft to its final synthesis
    location when done.

- id: author
  name: Author the slides
  description: |
    Map each audience-section to ONE source artefact. Cut sections
    that have no clear source for short formats; add placeholders
    for backup slides in long formats.

- id: speaker_notes
  name: Speaker notes on EVERY slide
  description: |
    Every slide gets >=1 sentence of notes. For figure slides, the
    note names the one feature to point the audience at.

- id: q_and_a
  name: Build the Q&A anticipation list
  description: |
    Anticipate the questions this audience will actually ask — each
    with a one-bullet answer outline + a backup slide jump. Cover the
    likely objections; a deck with no Q&A prep is half-finished.
    Sources — workspace/<step>/conclusions.md
    Limitations sections, workspace/logs/audit_report.md, mem_log
    close-call decisions, sys_state_get dead-end paths.

- id: audit
  name: Audit
  description: |
    tool_synthesis_scaffold (operation=check)(file='synthesis/slides.typ') catches short
    decks, missing speaker notes, citation over-cap, path leaks.

- id: compile
  name: Compile to PDF
  description: |
    tool_typst_compile(source='synthesis/slides.typ') renders
    synthesis/slides.pdf.

- id: dry_run
  name: Dry-run guidance
  description: |
    Tell the researcher to time themselves. If it's a talk in <72h,
    suggest a recording to spot pacing problems the deck alone
    won't show.

tools: |
  - tool_synthesis_scaffold (operation=plan) — what's ready
  - tool_synthesis_scaffold(kind='slides') — seed Touying starter
  - tool_synthesis_scaffold (operation=check) — slide-count + speaker-notes + path-leak audit
  - tool_synthesis_scaffold (operation=curate_figures) — TALK-version figures
  - tool_typst_compile — render synthesis/slides.pdf

expected_outputs:
- synthesis/slides.typ
- synthesis/slides.pdf
- synthesis/figures/                    # shared with paper.typ

next_protocol:
next_protocol_kind: terminal
on_failure: guidance/dead_end_routing
intent_class: synthesize
sub_intent: slides
triggers:
- make slides
- make me slides
- build the talk
- build my talk
- build a slide deck
- slide deck
- lab meeting deck
- conference slides
- defense talk
- defense slides
- powerpoint
- beamer
- marp
- reveal.js
- presentation deck
- deck for my talk
- deck for tomorrow
- i'm presenting
- job talk
- invited seminar
summary: Build a presentation deck (lab meeting / conference / defense / invited / teaching).
token_estimate: 2000
decomposition:
- protocol: synthesis/synthesis_slides
  purpose: Pick audience + outline + design principles.
- tool: tool_synthesis_scaffold
  args:
    kind: slides
- tool: tool_synthesis_scaffold (operation=check)
  args:
    file: synthesis/slides.typ
- tool: tool_typst_compile
  args:
    source: synthesis/slides.typ
